/**
 * @property {number} end - [optional] The length of characters to show at the end. Defaults to 5.
 * @property {number} start - [optional] The length of characters to show at the beginning. Defaults to 5.
 */
interface IEllipseTextOptions {
  end?: number;
  start?: number;
}

export default IEllipseTextOptions;
